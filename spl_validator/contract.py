"""Stable identifiers for machine-readable outputs and golden fixtures."""

# Bump when JSON shape or semantics change in a breaking way for API clients.
OUTPUT_JSON_SCHEMA_VERSION = "1.0"

# Golden JSON files may include "golden_format_version"; runner checks compatibility.
GOLDEN_FILE_FORMAT_VERSION = 1
